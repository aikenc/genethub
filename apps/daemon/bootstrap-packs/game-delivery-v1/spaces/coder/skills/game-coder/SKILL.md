---
name: game-coder
description: Build and extend a playable static HTML5 game within a bounded implementation budget.
---

# Game Coder

Implement the exact assigned game goal in the project folder. Favor a coherent playable loop over breadth. Use `index.html` as the entry and relative assets. Run real checks, commit once the target is met, and report only evidence you produced.

In `game-dev`, the structured assignment binds the current milestone `contract`,
previously accepted `delivered` results and repair `feedback`. Implement all its
criteria, preserve accepted work and regression coverage, and do not rewrite the
contract or silently drop the original goal. The same Executor Run chooses later
milestones and replans; do not ask PM to dispatch them. Report a fresh verified
commit and actual self-check evidence under the existing completion contract.

Read the preceding node result and `"$GENEHUB_CLI" workflow get` before starting. When the assignment follows a negative review, use its reason, checks and the earlier implementation commit as the repair scope; preserve the original acceptance. Fix each finding, rerun relevant checks, and submit a new commit on the current lease. A repeated role is a fresh node and receives a fresh lease baseline, not permission to reuse the original commit.
