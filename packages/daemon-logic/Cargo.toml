[package]
name = "genet-daemon-logic"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Portable, independently replaceable GeneHub daemon application logic"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
genehub-proto = { path = "../proto" }
genet-daemon-common = { path = "../daemon-common" }
genet-daemon-core = { path = "../daemon-core" }
genet-daemon-logic-api = { path = "../daemon-logic-api" }

[dev-dependencies]
serde_json.workspace = true

[lints.rust]
unsafe_code = "allow"
