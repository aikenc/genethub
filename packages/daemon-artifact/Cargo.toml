[package]
name = "genet-daemon-artifact"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Release tool for producing one signed GeneHub daemon Wasm file"

[dependencies]
base64.workspace = true
ed25519-dalek.workspace = true
genet-daemon-platform = { path = "../daemon-platform" }

[lints.rust]
unsafe_code = "forbid"
