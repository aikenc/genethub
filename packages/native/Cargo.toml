[package]
name = "genet-native"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "What only a native process can answer: OS confinement and where a program is installed"

[lib]
path = "src/lib.rs"

[dependencies]
genehub-proto = { path = "../proto" }
anyhow.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4"
